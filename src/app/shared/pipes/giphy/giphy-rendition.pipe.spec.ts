import { GiphyRenditionPipe } from './giphy-rendition.pipe';

describe('GiphyRenditionPipe', () => {
  it('create an instance', () => {
    const pipe = new GiphyRenditionPipe();
    expect(pipe).toBeTruthy();
  });
});
